# Tasks
- [x] Task 1: Update `tsconfig.app.json` include array: Change the TS include patterns to explicit relative paths starting with `./src` to ensure Volar properly registers and resolves Vue files from `src/components`.
  - [x] SubTask 1.1: Edit `frontend/tsconfig.app.json` and change `"include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"]` to `"include": ["./src/**/*.ts", "./src/**/*.tsx", "./src/**/*.vue"]`

# Task Dependencies
- No dependencies
